selectAll:
SELECT *
FROM Objects
ORDER BY objectID;

selectSince:
SELECT *
FROM Objects
WHERE objectID > ?
ORDER BY objectID;

insert:
INSERT INTO Objects(name)
VALUES (?);

-- todo: remove, update, whatever
