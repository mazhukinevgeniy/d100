selectAll:
SELECT *
FROM Collections
ORDER BY collectionID;

selectSince:
SELECT *
FROM Collections
WHERE collectionID > ?
ORDER BY collectionID;

insert:
INSERT INTO Collections(name)
VALUES (?);

-- todo: remove, update, whatever
